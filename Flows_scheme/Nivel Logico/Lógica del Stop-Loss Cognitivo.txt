@startuml
title Lógica Detallada del Stop-Loss Cognitivo

start
:Recibir Artefactos de la Iteración **t**;

partition "Cálculo de Ganancia (Positivo)" {
    :Evaluar **Relevancia** (Similitud con raíz);
    :Medir **Novedad** (Info nueva vs previa);
    :Calcular **Coverage_Gain** (KPIs resueltos);
    :Sumar **Contradiction_Resolution**;
}

partition "Cálculo de Riesgo/Gasto (Negativo)" {
    :Calcular **Drift_Risk** (Deriva semántica);
    :Medir **Redundancia** (Repetición);
    :Cuantificar **Costo Tokens** e **Inter-calls**;
}

:Aplicar Fórmula:
**Score = (Utilidad) - (Sobrecosto)**;

note right
  Formula Ref:
  StopLossScore = (R*0.3 + N*0.2 + G*0.25 + CR*0.15) 
  - (Drift*0.3 + Redundancy*0.2 + Cost*0.2)
end note

if (¿Score >= 0.18?) then (Sí)
    if (¿Hay Presupuesto disponible?) then (Sí)
        :Estado: **CONTINUE**;
        :Sugerir nuevos sub-tópicos;
    else (No)
        :Estado: **STOP (Budget Cap)**;
    endif
else (No)
    :Estado: **STOP (Marginal Utility)**;
endif

stop
@enduml